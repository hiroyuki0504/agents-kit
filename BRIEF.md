# 設計ブリーフ: マルチAIセッション協調キット（agents-kit）

作成: 2026-08-30。これは設計の入力。最終仕様は SPEC.md に書く。

## 目的

1つのGitHubリポジトリに対し、**別々のターミナルセッションで動く複数のAI**（Claude Code / Codex、モデル・effortは混在）が、人間の随時指示を受けながら、**コンフリクトを起こさず並行実装**できるようにする。

## 絶対要件

1. **AI非依存**: 調整の仕組みは git + 汎用CLI（python3スクリプト）+ gh だけで完結する。Claude固有機能（セッション間メッセージ等）に依存しない。Codexも同じ手順で参加できる。
2. **後勝ち原則**: ユーザーが後から出した指示が正。矛盾する古い指示に基づく作業は、議論なく破棄・巻き戻す。優先順位の全順序が機械的に決まること（時計に頼らない方法を推奨: 調整用ブランチのコミット順は線形なので全順序になる）。
3. **人が指示→調整は自動**: ユーザーはどのセッションに何を言ってもよい。担当宣言・衝突検知・順番待ち・マージ整列はAI同士がスクリプト経由で自動的に行う。
4. **構造で防ぐ**: 「AIが行儀よくする」に頼らない。ルール違反が物理的にできない/即検知される構造（アトミックなclaim、mainへの直接push禁止、マージの直列化）をスクリプトで強制する。

## 実際の運用形（想定シナリオ）

- ユーザーはこのMacで同じrepoのディレクトリに複数ターミナルを開き、Claude Code 2枚 + Codex 1枚などを起動する。
- 各セッションに口頭で指示（「ログイン画面作って」「あ、やっぱりボタンは赤で」）。指示は別セッションと矛盾しうる。
- 各AIは作業前にプロトコルに従って指示を台帳に記録し、担当を宣言し、自分のworktreeで実装し、直列化されたマージ手順でmainに入れる。

## 過去の実敗（設計に必ず織り込む。全て実際に起きた）

1. **意味衝突**: 別ファイル同士でもGitはきれいにマージするが意味が衝突する（初期化ロジックが4通り併存しコンパイル不能、検証ロジックが打ち消し合う）。21PR並行マージで4回発生。→ マージは直列化し、**1マージごとに**フルビルド+全テスト。
2. **gh pr merge が静かにコードを消す**: コンフリクト解決時に関数定義ごと消して成功を返すことがある。→ マージ前にローカルでマージをシミュレーションし内容確認（消えたシンボルが無いか）。確認後にのみ本マージ。
3. **worktree残留**: 別セッションが残した未コミット1,100行が共有worktreeに残留し誤診断。→ マージは**毎回クリーンな専用worktree**で行い、開始前に `git status --porcelain` が空であることを強制。
4. **worktreeのSHA固定**: worktreeは「今checkoutされているもの」からでなく、明示した `origin/main` のSHAから作る。
5. **マージ順序**: GitHubのmergeable判定は各PR単独vs main。全部mergeableでも順序次第で壊れる。→ 1本ずつマージし、残りはrebase+再テスト。
6. **ブランチ削除ハザード**: マージ直後にブランチを消すとマージ未確定のままPRが取り残されることがある。→ マージコミットがorigin/mainに載ったのを確認してから削除。
7. **積みPRのbase罠**: baseブランチを消すと子PRが静かに迷子になる。gh は成功を返す。→ 積みPRは原則禁止 or baseの生存を検査。
8. **rebase後は必ずローカル全テストを通してからpush**（4回踏んだ）。

## 提案する中核メカニズム（設計者はこれを土台に、穴を塞ぎ、詳細を確定させる）

### 調整媒体 = 専用ブランチ `agent-state`（orphan）
- 中身: `directives/`（指示台帳・1指示1ファイル）、`claims/`（担当宣言・1担当1ファイル）、`log/`（完了記録）。
- **アトミック性**: 状態変更は「commit → push」。pushはfast-forward必須なので**git自体がCAS**になる。push拒否 = 誰かが先に状態を変えた合図（エラーではなく正常な調整イベント）。fetch→再読→リトライ。
- ファイル名は衝突しない設計（セッションID・タイムスタンプ入り）にし、状態ブランチ上のテキストコンフリクト自体を原理的に排除する。
- 後勝ちの全順序 = `agent-state` ブランチのコミット順。

### CLI `agents`（python3単一ファイル、標準ライブラリのみ）
サブコマンド案（設計者が確定させる）:
- `agents directive "<指示原文>"` — ユーザー指示を台帳に記録（原文+解釈スコープ）。全ての作業はどれかのdirectiveに紐づく。
- `agents start <slug> --paths <glob,...> --intent "<1行>"` — claim（CAS）+ `origin/main` SHA固定のworktree作成 + ブランチ `agent/<session>/<slug>` を一体で行う。パス重複するclaimが既にあれば拒否し、相手のintentを表示。
- `agents sync` — fetch + 現在のclaims/最新directives表示。**新しいdirectiveが自分のスコープと矛盾していないかの確認を作業者に義務づける**。
- `agents status` — 全claim・経過時間・ブランチ・PR一覧。
- `agents done` — rebase→全テスト→push→PR作成（gh無しなら直マージモード）。
- `agents merge <pr|branch>` — グローバルmergeロック取得→クリーン専用worktreeでローカルマージシミュレーション→シンボル消失チェック→全テスト→本マージ→origin/main反映確認→遅延ブランチ削除→ロック解放→claim解除→log記録。
- `agents release <slug>` / `agents evict` — 解放 / TTL切れclaimの掃除（死んだセッション対策）。
- claimにはTTLとrefreshed_at（コマンド実行のたびに自動更新）。mergeロックは短TTL。

### 導入形
- `install.sh <target-repo>` で `.agents/PROTOCOL.md` と `.agents/bin/agents` を配置し、`AGENTS.md`（Codexが読む）と `CLAUDE.md`（Claudeが読む）にマーカー付きポインタブロックを追記。orphanブランチ `agent-state` をoriginに作成。冪等。**グローバル設定（~/.claude等）には一切触れない**。
- `PROTOCOL.md` はAIが読む前提で書く: 短い命令形ルール、正常系1本道、異常系プレイブック（push拒否されたら/claim衝突したら/新directiveと矛盾したら/mergeロックが古いまま残っていたら）。

## 設計者が確定させるべき未決事項

1. `agent-state` の読み書き実装: 隠しworktree方式（`.git/agents-state-wt`、デバッグ容易）vs plumbing方式（commit-tree、クリーン）。多セッションが**同一クローンを共有**するケース（ユーザーが同じディレクトリで複数ターミナルを開く）で安全な方。
2. セッション識別: 環境変数か、start時に生成しworktree内の未追跡ファイルに永続化か。claim前に識別子が要る問題の解決（start一体化で解決するか）。
3. パス重複判定の粒度: glob交差 + 「触るインターフェース」宣言（意味衝突の早期警告）をどこまでやるか。過剰設計にしない。
4. directiveの矛盾判定: 機械判定は不可能（意味の問題）。**いつ・誰が・何を根拠に**確認するかをプロトコルに固定する（例: start時とdone/merge前の2回、sync出力に「自分のclaim以降に増えたdirective」を強調表示し、AIに矛盾判断を義務づける）。
5. テストコマンドの規約: repoごとに違う。`.agents/config`（例: `test_cmd`）で注入。未設定時の挙動。
6. worktree配置場所: 兄弟ディレクトリ vs repo内。Codexのサンドボックスがcwd外書き込みを制限する場合の考慮。
7. gh無し環境のフォールバック（直マージモード）の具体形。
8. mainへの直接push防止: クライアント側フック？ それともプロトコル+mergeコマンド経由のみという規律？（GitHub branch protectionは有料/設定依存なので前提にしない。ただし推奨設定として文書化はする）

## 成功基準（後続の実証フェーズで検証する）

- 同時claim競争20回で毎回勝者がちょうど1人、敗者は正しく検知して退避。
- 3セッション並行（うち2つは同一ファイルに触れたがる）で、全部が直列マージされ、各マージ後にテスト緑、コード消失なし。
- 矛盾シナリオ: directive D1で作業中にセッションBが矛盾するD2を記録 → 作業者Aがmerge前syncで検知し、D2に従って作り直す。最終状態はD2準拠（後勝ちの実証）。
- 死んだセッションのclaimがTTL後にevictされ、他セッションが引き継げる。
- 実GitHub上でのPRフロー（作成→シミュレーション→マージ→反映確認）が通る。

## 非目標

- タスクキューからの完全自律pickup（将来拡張として構造だけ壊さない）。
- GitHub branch protection / merge queue 等の有料・設定依存機能を前提にすること。
- Claude間のリアルタイムメッセージング（使えれば速いが、正はあくまでgit上の状態）。
